#include<bits\stdc++.h>
using namespace std;

int main()
{
    int n;
    char str1[100];
    string str2;
    while(cin>>n){
        int count1=0,count2=0,count3=0,count4=0,;
        while(n--){
            scanf("%s",str1);
            getline(cin,str2);
        }
        if(str1=="Italy"){
            count1++;
        }

        if(str1=="England"){
            count++;
        }
    }
    return 0;
}
